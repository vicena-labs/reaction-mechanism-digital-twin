# Rowan workflows
Paid stages use Compute Shell runners, not notebooks. Store workflow UUID, task key, endpoint hashes, method and settings, charge, multiplicity, solvent, temperature, atom order, credit cap, status, timestamps, result paths, and acceptance outcome. `state/rowan_workflows.json` is durable deduplication state and must not be deleted.
